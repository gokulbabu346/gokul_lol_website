<footer class="footer">
  <div class="wrap grid">
    <p>© 2026 Gokul D_Bog. Every figure on this page reconciles. Probably.</p>
    <p class="mono dim tag">ASTRO × HONO × CLOUDFLARE WORKERS</p>
    <a class="health mono" href="/health" id="health-pill" title="GET /health">
      <span class="dot"></span><span id="health-text">/health → CHECKING…</span>
    </a>
  </div>
</footer>

<style>
  .footer {
    border-top: 1px solid var(--line);
    padding: 30px 0;
    margin-top: 40px;
  }
  .grid {
    display: flex;
    flex-wrap: wrap;
    gap: 14px 28px;
    align-items: center;
    justify-content: space-between;
    color: var(--dim);
    font-size: 13px;
  }
  .tag {
    font-size: 11px;
    letter-spacing: 0.18em;
  }
  .health {
    display: inline-flex;
    align-items: center;
    gap: 9px;
    border: 1px solid var(--line);
    border-radius: 999px;
    padding: 8px 14px;
    font-size: 11px;
    letter-spacing: 0.08em;
    color: var(--muted);
    transition: border-color 0.2s;
  }
  .health .dot {
    background: var(--dim);
    box-shadow: none;
    animation: none;
  }
  .health.ok {
    border-color: rgba(61, 220, 151, 0.4);
  }
  .health.ok .dot {
    background: var(--green);
    box-shadow: 0 0 10px rgba(61, 220, 151, 0.8);
    animation: pulse 2.2s infinite;
  }
</style>
