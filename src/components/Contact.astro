<section class="section" id="contact">
  <div class="wrap inner">
    <p class="eyebrow reveal">✦ Send Word ✦</p>
    <h2 class="reveal">Summon the Accountant.</h2>
    <p class="lede reveal">
      A ledger that needs taming? A variance on the loose? Or chartership intel to trade?
      Transmission lines are open — response time guaranteed faster than a year-end close.
    </p>
    <div class="cta reveal">
      <a class="btn btn-primary" href="mailto:reconcile@gokul.lol">Send a Raven (Email)</a>
      <a class="btn btn-ghost" href="https://www.instagram.com/gokul_d_bog" target="_blank" rel="noopener">Instagram — @gokul_d_bog</a>
      <a class="btn btn-ghost" href="https://wa.me/918129639554" target="_blank" rel="noopener">WhatsApp</a>
    </div>
    <p class="details mono reveal">
      <a href="tel:+918129639554">+91 8129 639 554</a>
      <span class="sep">·</span>
      <a href="mailto:reconcile@gokul.lol">reconcile@gokul.lol</a>
    </p>
  </div>
</section>

<style>
  .inner {
    text-align: center;
    display: flex;
    flex-direction: column;
    align-items: center;
  }
  h2 {
    font-size: clamp(2.2rem, 6vw, 3.6rem);
    letter-spacing: -0.02em;
  }
  .lede {
    max-width: 560px;
    color: var(--muted);
    font-size: 1.08rem;
    margin-top: 18px;
  }
  .cta {
    display: flex;
    gap: 14px;
    justify-content: center;
    flex-wrap: wrap;
    margin-top: 38px;
  }
  .details {
    margin-top: 28px;
    color: var(--muted);
    font-size: 13px;
    letter-spacing: 0.06em;
  }
  .details a {
    transition: color 0.2s;
  }
  .details a:hover {
    color: var(--gold);
  }
  .details .sep {
    opacity: 0.5;
    margin: 0 10px;
  }
</style>
